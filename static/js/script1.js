// let currentIndex = 0;
//       const user = {{ user|safe }};
//       const button = document.getElementById('add');
//       const newsItem = document.getElementById('news-item');

//       button.addEventListener('click', () => {
//         currentIndex++;
//         if (currentIndex < user.length) {
//           newsItem.textContent = `${user[currentIndex].id}:${user[currentIndex].task}`;
//         } else {
//           button.disabled = true; // Disable button if no more items
//         }
//       });